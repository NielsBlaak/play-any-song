# Spotify Web API — Client ID for PKCE OAuth flow.
# Create an app at https://developer.spotify.com/dashboard, then paste the Client ID below.
# No client secret needed — PKCE flow doesn't use one in the browser.
VITE_SPOTIFY_CLIENT_ID=your_spotify_client_id_here
